Subject: Server Access Information Needed - Placement Management System



 Currently it runs on cloud services (Vercel, Render, TiDB,cloudinary). I need the following information to proceed:

1. SERVER ACCESS
   - SSH username and password/key
   - Server IP address

2. OPERATING SYSTEM
   - OS type (Ubuntu/Debian/CentOS) and version

3. NODE.JS & PACKAGES
   - Node.js installed? (version?)
   - Can we install Node.js v18 or v20?
   - npm and PM2 available?

4. DATABASE
   - Database system (MySQL/MariaDB/PostgreSQL)
   - Root password for initial setup

5. WEB SERVER
   - Nginx or Apache installed?
   - Can we configure reverse proxy?
   - Ports 80 and 443 open?

6. DOMAIN & HTTPS
   - Domain/subdomain available (example: placement.college.edu)
   - HTTPS/SSL support?

7. FILE STORAGE
   - Can we use Cloudinary API for uploads?
   - Outbound connections allowed?

8. SYSTEM RESOURCES
   - Available RAM (2GB minimum)
   - Storage space (20GB minimum)

9. DEPLOYMENT
   - How should code be uploaded? (Git/FTP/SCP)
   - Can we access GitHub from the server?

10. EMAIL SERVICE
    - Can we use external APIs? (Gmail SMTP, Resend, SendGrid)
    - Or college SMTP details if available

Please provide these details. A detailed checklist is attached for reference.


